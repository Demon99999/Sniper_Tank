using UnityEngine;

namespace Assets.Scripts.Infrastructure.Factoris.TankFactory
{
    public class TankFactoryInstaller : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
